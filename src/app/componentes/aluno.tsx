
import CountingComponent from '../componentes/CountingComponent';

const HomePage = () => {
  const initialValue = 148;

  return (
      <CountingComponent initialValue={initialValue} />
  );
};

export default HomePage;
